module.exports = {
  soma(a, b) {
    let soma = a + b;
    console.log(`O resultado da soma '${a} + ${b}' é '${soma}'.`);
  },

  multiplicacao(a, b) {
    let mp = a * b;
    console.log(`O resultado da soma '${a} * ${b}' é '${mp}'.`);
  }
}