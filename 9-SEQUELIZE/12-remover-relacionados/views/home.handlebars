<h1>Usuários cadastrados</h1>
{{#if users}}
  <div class="container">
  {{#each users}}
    <div class="item">
      <h1>{{this.name}}</h1>
      <p>{{this.occupation}}</p>
      <a href="/users/{{this.id}}"><button class="form-btn">Ver perfil</button></a>
      <a href="/users/edit/{{this.id}}"><button class="form-btn form-seccond-btn">Editar</button></a>
      <form action="/users/delete/{{this.id}}" method="POST">
        <input type="submit" value="Excluir" class="form-btn form-seccond-btn">
      </form>
    </div>
  {{/each}}
  </div>
{{else}}
  <p>Nenhum usuário foi cadastrado ainda.</p>
{{/if}}